# Settings for Backend (on Cloud Run).
# See https://firebase.google.com/docs/app-hosting/configure#cloud-run
runConfig:
  minInstances: 0
  # maxInstances: 100
  # concurrency: 80
  # cpu: 1
  # memoryMiB: 512

# Environment variables and secrets.
# Note: Firebase App Hosting will use the environment variables from .env.local
# These are kept as fallback configuration
env:
  # Firebase Configuration for Production Deployment
  - variable: NEXT_PUBLIC_FIREBASE_API_KEY
    value: AIzaSyA8HYKjwg20P5zqUBwZaFM8VqPsxJX4rfA
    availability:
      - BUILD
      - RUNTIME
  
  - variable: NEXT_PUBLIC_FIREBASE_PROJECT_ID
    value: master-photocopy
    availability:
      - BUILD
      - RUNTIME
  
  - variable: NODE_ENV
    value: production
    availability:
      - BUILD
      - RUNTIME
